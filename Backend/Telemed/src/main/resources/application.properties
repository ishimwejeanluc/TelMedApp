spring.application.name=Telemed


# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/telemed
spring.datasource.username=postgres
spring.datasource.password=Lukatoni123


# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional: Set Hibernate Dialect for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SMTP server configuration for sending emails
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=Lukatoni442@gmail.com
spring.mail.password=afyjclwthpgztelk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
# Optional: Configure default encoding
spring.mail.default-encoding=UTF-8

# Optional: Configure protocol (SMTP is default)
spring.mail.protocol=smtp

# Email encryption key (must be exactly 16 characters)
app.email.encryption.key=TelemedApp@2024!
